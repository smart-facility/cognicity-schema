dist: trusty
language: node_js
node_js:
   - "7"
sudo: false

addons:
  postgresql: "9.6"
  apt:
    packages:
      - postgresql-9.6-postgis-2.3

branches:
  only:
    - master
    - dev
    - chennaiChanges

before_install: bash ./build/run.sh

install:
  - npm install
